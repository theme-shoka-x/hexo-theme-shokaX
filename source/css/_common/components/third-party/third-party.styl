@import 'pace';
@import 'loading';
@import 'search';
@import 'theme';
@import 'widgets';

.katex,
.katex-display {
    user-select: all;
}

.medium-zoom-overlay {
  z-index: $zindex-5 - 1;
}

.medium-zoom-image--opened {
  z-index: $zindex-5;
}

.lozaded {
  @extend .blur;
}

@css {
  .wl-emoji-popup {
    max-width: min(calc(100vw - 100px), 526px) !important;  
  }
}